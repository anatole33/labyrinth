#include <stdlib.h>
#include <stdio.h>
#include "maze_functions.h"

int main(int argc, char * argv[]){
    g = game_create;
    // get pointer on the game
    game *current_game*= &g;

    printf('yolo')

    game_delete(current_game);
    return 0;
}
